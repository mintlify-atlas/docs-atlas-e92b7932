---
title: 'Roles'
description: 'Manage team roles and permissions for your agency'
api: 'GET https://api.blutui.com/v1/agencies/{username}/roles'
---

## List all roles

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField query="limit" type="integer" default={10}>
  Limit the number of objects to return, ranging between 1 and 20.
</ParamField>

<ParamField query="page" type="integer" default={1}>
  The current page used to display the pagination results.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').roles.list()
```

```bash cURL
curl -X GET 'https://api.blutui.com/v1/agencies/studio/roles' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

<ResponseField name="object" type="string">
  This will always contain `list` for this endpoint.
</ResponseField>

<ResponseField name="data" type="array">
  An array of up to `limit` role resources. If no roles are available, an empty array is returned.
</ResponseField>

<ResponseField name="meta" type="object">
  Additional information about the list.
</ResponseField>

---

## Create role

**`POST https://api.blutui.com/v1/agencies/{username}/roles`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').roles.create({
  name: 'Editor',
  permissions: ['edit_content', 'view_content']
})
```

```bash cURL
curl -X POST 'https://api.blutui.com/v1/agencies/studio/roles' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Retrieve role

**`GET https://api.blutui.com/v1/agencies/{username}/roles/{id}`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField path="id" type="string" required>
  The ID of the role to retrieve.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').roles.get('role-id')
```

```bash cURL
curl -X GET 'https://api.blutui.com/v1/agencies/studio/roles/role-id' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Update role

**`PATCH https://api.blutui.com/v1/agencies/{username}/roles/{id}`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField path="id" type="string" required>
  The ID of the role to update.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').roles.update('role-id', {
  name: 'Senior Editor'
})
```

```bash cURL
curl -X PATCH 'https://api.blutui.com/v1/agencies/studio/roles/role-id' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Remove role

**`DELETE https://api.blutui.com/v1/agencies/{username}/roles/{id}`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField path="id" type="string" required>
  The ID of the role to remove.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').roles.remove('role-id')
```

```bash cURL
curl -X DELETE 'https://api.blutui.com/v1/agencies/studio/roles/role-id' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>