---
title: 'Invites'
description: 'Manage team invitations for your agency'
api: 'GET https://api.blutui.com/v1/agencies/{username}/invites'
---

## List all invites

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField query="limit" type="integer" default={10}>
  Limit the number of objects to return, ranging between 1 and 20.
</ParamField>

<ParamField query="page" type="integer" default={1}>
  The current page used to display the pagination results.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').invites.list()
```

```bash cURL
curl -X GET 'https://api.blutui.com/v1/agencies/studio/invites' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

<ResponseField name="object" type="string">
  This will always contain `list` for this endpoint.
</ResponseField>

<ResponseField name="data" type="array">
  An array of up to `limit` invite resources. If no invites are available, an empty array is returned.
</ResponseField>

<ResponseField name="meta" type="object">
  Additional information about the list.
</ResponseField>

---

## Update invite

<api>PATCH https://api.blutui.com/v1/agencies/{username}/invites/{id}</api>

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField path="id" type="string" required>
  The ID of the invite to update.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').invites.update('invite-id', {
  // update parameters
})
```

```bash cURL
curl -X PATCH 'https://api.blutui.com/v1/agencies/studio/invites/invite-id' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Remove invite

<api>DELETE https://api.blutui.com/v1/agencies/{username}/invites/{id}</api>

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField path="id" type="string" required>
  The ID of the invite to remove.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').invites.remove('invite-id')
```

```bash cURL
curl -X DELETE 'https://api.blutui.com/v1/agencies/studio/invites/invite-id' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>