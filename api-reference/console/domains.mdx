---
title: 'Domains'
description: 'Manage custom domains for your projects'
api: 'GET https://api.blutui.com/v1/agencies/{username}/domains'
---

## List all domains

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField query="limit" type="integer" default={10}>
  Limit the number of objects to return, ranging between 1 and 20.
</ParamField>

<ParamField query="page" type="integer" default={1}>
  The current page used to display the pagination results.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').domains.list()
```

```bash cURL
curl -X GET 'https://api.blutui.com/v1/agencies/studio/domains' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

<ResponseField name="object" type="string">
  This will always contain `list` for this endpoint.
</ResponseField>

<ResponseField name="data" type="array">
  An array of up to `limit` domain resources. If no domains are available, an empty array is returned.
</ResponseField>

<ResponseField name="meta" type="object">
  Additional information about the list.
</ResponseField>

---

## Create domain

**`POST https://api.blutui.com/v1/agencies/{username}/domains`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').domains.create({
  name: 'example.com',
  project: 'project-id'
})
```

```bash cURL
curl -X POST 'https://api.blutui.com/v1/agencies/studio/domains' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Retrieve domain

**`GET https://api.blutui.com/v1/agencies/{username}/domains/{id}`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField path="id" type="string" required>
  The ID of the domain to retrieve.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').domains.get('domain-id')
```

```bash cURL
curl -X GET 'https://api.blutui.com/v1/agencies/studio/domains/domain-id' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Update domain

**`PATCH https://api.blutui.com/v1/agencies/{username}/domains/{id}`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField path="id" type="string" required>
  The ID of the domain to update.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').domains.update('domain-id', {
  // update parameters
})
```

```bash cURL
curl -X PATCH 'https://api.blutui.com/v1/agencies/studio/domains/domain-id' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Search domains

**`GET https://api.blutui.com/v1/agencies/{username}/domains/search`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').domains.search({
  query: 'example'
})
```

```bash cURL
curl -X GET 'https://api.blutui.com/v1/agencies/studio/domains/search?query=example' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Verify domain

**`POST https://api.blutui.com/v1/agencies/{username}/domains/{id}/verify`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField path="id" type="string" required>
  The ID of the domain to verify.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').domains.verify('domain-id')
```

```bash cURL
curl -X POST 'https://api.blutui.com/v1/agencies/studio/domains/domain-id/verify' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Refresh domain

**`POST https://api.blutui.com/v1/agencies/{username}/domains/{id}/refresh`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField path="id" type="string" required>
  The ID of the domain to refresh.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').domains.refresh('domain-id')
```

```bash cURL
curl -X POST 'https://api.blutui.com/v1/agencies/studio/domains/domain-id/refresh' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Remove domain

**`DELETE https://api.blutui.com/v1/agencies/{username}/domains/{id}`**

<ParamField path="username" type="string" required>
  The agency username.
</ParamField>

<ParamField path="id" type="string" required>
  The ID of the domain to remove.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.agency('studio').domains.remove('domain-id')
```

```bash cURL
curl -X DELETE 'https://api.blutui.com/v1/agencies/studio/domains/domain-id' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>