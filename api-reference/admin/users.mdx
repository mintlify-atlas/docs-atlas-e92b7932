---
title: 'Users'
description: 'Manage users for your project'
api: 'GET https://{handle}.blutui.com/admin/api/users'
---

## List all users

<ParamField query="limit" type="integer" default={10}>
  Limit the number of objects to return, ranging between 1 and 20.
</ParamField>

<ParamField query="page" type="integer" default={1}>
  The current page used to display the pagination results.
</ParamField>

<ParamField query="expand" type="string[]">
  Expand the response for the given items. Expand array accepts `list`.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.project('one').admin.users.list()
```

```bash cURL
curl -X GET 'https://{handle}.blutui.com/admin/api/users' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

<ResponseField name="object" type="string">
  This will always contain `list` for this endpoint.
</ResponseField>

<ResponseField name="data" type="array">
  An array of up to `limit` user resources. If no users are available, an empty array is returned.
</ResponseField>

<ResponseField name="meta" type="object">
  Additional information about the list.
</ResponseField>

---

## Retrieve user

**`GET https://{handle}.blutui.com/admin/api/users/{id}`**

<ParamField path="id" type="string" required>
  The ID of the user to retrieve.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.project('one').admin.users.get('user-id')
```

```bash cURL
curl -X GET 'https://{handle}.blutui.com/admin/api/users/user-id' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>

---

## Remove user

**`DELETE https://{handle}.blutui.com/admin/api/users/{id}`**

<ParamField path="id" type="string" required>
  The ID of the user to remove.
</ParamField>

<CodeGroup>
```typescript Node.js
import Blutui from 'blutui'

const blutui = new Blutui('ey....')

blutui.project('one').admin.users.remove('user-id')
```

```bash cURL
curl -X DELETE 'https://{handle}.blutui.com/admin/api/users/user-id' \
     -H 'Authorization: Bearer ey....' \
     -H 'Content-Type: application/json'
```
</CodeGroup>